/** @type {import('next').NextConfig} */
const nextConfig = {
    // TODO: need to configure the protocol, hostname, port, and pathname for Next.js Image to use external Url
};

export default nextConfig;
